/// Résultat du test de positionnement (domaine).
class PlacementResult {
  const PlacementResult({
    required this.correctCount,
    required this.unitsUnlocked,
  });

  final int correctCount;
  final int unitsUnlocked;
}
